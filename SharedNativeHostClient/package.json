{
    "name": "shared-native-host-client",
    "private": true,
    "workspaces": [
        "packages/scripts",
        "packages/clientapis",
        "packages/clientsite"
    ],
    "scripts": {
        "_note": "ending in '--' allows command line arguments to be forwarded to the package scripts",
        "build": "npm run build --workspaces --if-present --",
        "clean": "npm run clean --workspaces --if-present"
    },
    "devDependencies": {
        "@types/node": "^22.5.4",
        "typescript": "^5.6.2"
    }
}
